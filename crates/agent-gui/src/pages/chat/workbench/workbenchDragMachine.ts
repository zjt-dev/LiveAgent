// The workbench drag state machine is shared with the WebUI; the
// implementation lives in @liveagent/ui so both hosts resolve drop targets
// with identical semantics.
export * from "@liveagent/ui/lib/workbench/dragMachine";
