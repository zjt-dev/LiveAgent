// The pointer-driven drag session hook is shared with the WebUI; the
// implementation lives in @liveagent/ui alongside the drag state machine.
export * from "@liveagent/ui/lib/workbench/useWorkbenchDragSession";
